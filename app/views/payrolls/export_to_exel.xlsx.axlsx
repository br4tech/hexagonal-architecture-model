wb = xlsx_package.workbook
wb.add_worksheet(name: "Boletos") do |sheet|
  @bills.each do |bill|
    sheet.add_row ["Codigo", "Cliente", "Contrato"]
    sheet.add_row [bill[:id], bill[:client], bill[:kind]]
    sheet.add_row ["Cód. Reserva", "Unidade", "Sala", "Qtd. Horas", "Valor"]
    bill[:items].each do |item|
      sheet.add_row [item[:id], item[:office], item[:clinic], item[:hours], item[:amount]]
    end
  end
end